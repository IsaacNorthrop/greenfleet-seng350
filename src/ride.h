#include <stdio.h>
#include <string>

using namespace std;

class Ride {
    public:
        string driverName;
        string password;
        string licensePlate;
        string carDescription;
        string destination;
        string capacity;
        bool rideShare;
        bool servingCustomer;
    
};